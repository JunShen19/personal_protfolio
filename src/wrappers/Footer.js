import styled from "styled-components";

const Wrapper = styled.footer`
  .social-icons {
    display: flex;
    gap: 1rem;
  }
  .social-icon {
    font-size: 1rem;
    color: var(--textColor);
  }
`;

export default Wrapper;
